# Exemplo .env para o projeto Meu Emprego
# Copie este arquivo para `.env` e preencha os valores reais. NÃO versionar o .env com segredos.

# URI do MongoDB (opcional; se ausente, o app salva em candidaturas.csv)
MEU_EMPREGO_MONGO_URI="mongodb://usuario:senha@localhost:27017"

# Nome do banco (opcional)
MEU_EMPREGO_DB_NAME="meu_emprego"

# Caminho para o CSV de fallback (opcional)
CANDIDATURAS_CSV_PATH="candidaturas.csv"

# Ambiente
APP_ENV="development"
DEBUG=1
LOG_LEVEL="DEBUG"
